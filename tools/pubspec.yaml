name: napstack_tools
description: Server-side tooling for NapStack — schema provisioning, migrations.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  dart_appwrite: ^13.0.0
